# V0.1.1 真实集成加固 — 最终报告

仓库：`thesis-review-dsh-plugin`（分支 `main`）
主仓库 `thesis-review-agent`：**未修改**（HEAD 全程 `6a0773a`，`git status` 全程 clean）
本轮最终 commit：`d566c48`
运行环境：Node v22.19.0（DSH 官方要求 v22.19+/v24；沙箱默认 v20.18.0 会因 `Promise.withResolvers` / `node:zlib.createZstdDecompress` 缺失而无法启动 dsh-base，已改用 `/data/work/node-v22.19.0-linux-x64`）

---

## 一、真实发现并修复的 Bug

每个 Bug 都是先用真实运行复现、定位根因、做最小修复、再用同一个测试验证；不是照抄 checklist。

### Bug A — systemPrompt 未注入即读取，真实 DSH 启动崩溃
- **复现**：真实 `dsh web` 启动报 `cannot get property "systemPrompt" without inject`（plugin.js）。
- **根因**：Cordis 的 Context proxy 对“未声明 inject 的属性读取”会**抛异常**，而不是返回 undefined；原来的防御式 `typeof ctx.systemPrompt === 'function'` 根本没机会执行。faux ctx 太宽松，没暴露这个问题——只有真实 Harness 加载才复现。
- **修复**：用 `ctx.reflect.get('systemPrompt', false)`（Cordis 内部同款非严格探测）代替直接属性读取；`systemPrompt` 保持不在 `inject` 里（声明它会变成硬依赖，在 minimal profile 里卡死插件）。
- **验证**：新增回归测试“systemPrompt peer 缺失时仍能加载并注册 7 个工具”；真实 boot 0 error。
- **commit**：`a61e144`

### Bug B — 跨 Session worker 状态串档（Checklist 1 / 六）
- **复现**：真实 worker 下 `tests/_repro-isolation.mjs` 打印 `REPRO=LEAK CONFIRMED (A read B.docx)`——Session A 的 find_text 返回了 Session B 打开的支持性稿件文本。
- **根因**：DSH 用 mountRootInclude **只挂载插件一次**，所以 `apply()` 只跑一次，`WorkerSession` 被所有并发 agent/session 共享；而 thesis-review worker 是**有状态单稿件**（`self.opened` 等），导航类 op（list_outline/read_section/...）**不带 draft_id**，只读“当前稿件”。两个 session 共用一个 worker，B 的 `open_draft` 会覆盖 A 正在审的文档。
- **修复**：按 `exec.agent.id`（== SessionId）路由到**每 session 独立的 WorkerChannel**（各自一个 worker 进程）；监听 `agent/disposed` 回收对应 session 的 worker；插件卸载时 dispose 全部。无 agent 身份的调用回退到 `DEFAULT_SESSION_KEY`，保持单 session 行为不变。**没有自造复杂 Session framework**，只用 DSH 官方的 agent 身份 + 生命周期事件。
- **验证**：`tests/session-isolation.test.ts` 2 个真实 worker 测试通过（A 只见 A.docx，B 只见 B.docx；disposeSession('B') 后 A 不受影响、B 可惰性重启）。
- **commit**：`cf3d41c`

### Bug C — Worker 结构化错误码到不了模型（Checklist 3 / 八）
- **复现**：真实 scheduler dispatch（`observe-error.mjs`）里，编造 claim_quote 被 worker 拒绝，但模型可见结果是 `content:[{text:"Error: 主张或证据原文不在稿件中…"}]`、`error:{message}`，`WORKER_CODE_VISIBLE_TO_MODEL=false`——`quote_not_in_draft` 这个 code 丢了。
- **根因**：DSH 的 `toolErrorResult` 会把抛出的错误**拍平**成 `Error: <message>`，只有它自己的 `HarnessError` 才保留结构化 `error.code`；我们的 `WorkerError` 不是 HarnessError。所以“直接 execute 能拿到 err.code”≠“过了 ToolRegistry 后模型能看到 code”。
- **修复**：给 `WorkerError` 加 `domain` 标志（**只在协议边界**收到 worker 的结构化 `{error:{code,message}}` 时置 true）。`runWorkerOp` 对 domain 拒绝**返回**规范 JSON `{ok:false, error:{code,message}}`（走 json output schema，code 存活到模型）；对基础设施故障（worker 起不来、socket 死、session_closed）**仍然抛出**，让 Harness 标记为 error。
- **验证**：重新 dispatch 观察 `WORKER_CODE_VISIBLE_TO_MODEL=true`；Evidence Gate 未被削弱（worker 照样拒绝、不写批注）。
- **commit**：`715d76e`

### Bug D — stdio 启动永久挂起（Checklist 4 / 九）
- **复现**：`_repro-timeout.mjs` Test A：worker spawn 成功但 Python 卡住不回 readiness ping，`elapsed=5005ms -> HUNG`，尽管配了 `startupTimeoutMs=1500`。
- **根因**：`startStdioWorker` **根本没收到** `startupTimeoutMs`；readiness 探测没有任何超时，`startWorker()` 的 Promise 永远 pending。
- **修复**：把 `startupTimeoutMs` 传进 stdio 分支，并加 readiness 定时器，超时以 `worker_startup` 失败（clearTimeout + failAll）。
- **验证**：`elapsed=1502ms -> REJECTED: worker_startup`。
- **commit**：`b3bb273`

### Bug E — worker 崩溃后 EPIPE 崩宿主 + pending 永挂（Checklist 4 / 九）
- **复现**：`_repro-timeout.mjs` Test B：worker 回完 ping 后 `os._exit(1)`，下一次 call 时 Node 进程直接崩 `Unhandled 'error' event: write EPIPE`（往已退出 worker 的 stdin 写）。比预期更糟——不只是挂起，是崩溃。
- **根因**：`child.stdin` 没有 error handler；worker ready 后退出也没有 reject pending 调用。
- **修复**：`child.stdin.on('error', ()=>{})` 兜住 EPIPE；`call()` 的 write 用 try/catch，写失败转成 `worker_write_failed` 拒绝并 `failAll`；worker exit / stdout close / socket error+close 全部 `failAll` 拒绝所有 pending 与后续调用。TCP 分支同样加 `socket.on('error'/'close')` 与 `child.on('exit')`。
- **验证**：`elapsed=0ms -> REJECTED: worker_exit`，进程退出码 0（不再崩）。
- **commit**：`b3bb273`

### Bug F — Harness deadline 到了，工具却不理 `exec.signal`（Checklist 4 / 九）
- **复现/依据**：读官方源码确认机制——`dsh-tool-call-timeout-policy` 通过 `deadline(exec.signal, timeoutMs)` 用工具的 `timeoutMs` **武装** `exec.signal`，但 `dsh-timeout` 明说“**The signal only notifies, so callers must stop their own work**”；`dsh-tools` 类型注释也要求“Async work must observe or forward `exec.signal`”。而我们的 `execute` **完全没读** `exec.signal`，所以 deadline 触发后 worker 调用照样 pending，拖住整个 dispatch 超过 Harness 预算。
- **修复**：把 `exec.signal` 一路穿透 `runWorkerOp -> WorkerSession.call -> WorkerChannel -> LineProtocol.call`；signal 触发时**丢弃这一条 pending 调用**（`worker_aborted`），worker 进程保持存活（共享的有状态 per-session worker 不能因一次超时就毁掉打开的稿件），迟到的回包被 `feed()` 丢弃（id 已不在 pending）。调用前若 signal 已 aborted，直接拒绝不派发。
- **验证**：`tests/worker-resilience.test.ts` Test C（中途 abort → `worker_aborted`）、Test D（预先 aborted → 拒绝）通过。
- **commit**：`b3bb273`

---

## 二、Checklist 结论

| Checklist | 结论 | 依据 |
|---|---|---|
| Session 隔离（六） | **FIXED** | Bug B；`session-isolation.test.ts` 真实 worker 2 测通过；真实 boot 单行干净日志 |
| History workerHome（七） | **FIXED（fail closed）** | 见下 |
| 结构化 Worker 错误（八） | **FIXED** | Bug C；真实 scheduler dispatch 观察 code 存活 |
| 超时 / abort（九） | **FIXED** | Bug D、F；resilience 测 A/C/D 通过 |
| Worker 崩溃（九） | **FIXED** | Bug E；resilience 测 B 通过，无 EPIPE 崩溃 |
| Dispose / 无 orphan（九） | **PASS（加固后）** | resilience 测 E + disposeSession 测：dispose 后无残留 Python，后续调用得 `session_closed` |
| 真实 DSH 插件加载（五/十） | **PASS** | 见下“真实 DSH CLI 加载” |
| Evidence Gate 未削弱（十一） | **NOT AN ISSUE（保持）** | 7 个真实 worker 测试全绿；拒绝仍源自 Python worker |
| 真实模型 E2E（十二） | **NOT RUN** | 环境无 DSH 模型 API key（仅 browser-session grant） |
| package-lock 清理（十四） | **FIXED** | 见下 |

### Checklist 2（七）History workerHome — 详情
- **问题**：`workerHome` 原本不是 config 字段，`WorkerChannel.ensureStarted` 也从不传它，所以每个 worker 都用**全新临时 home**。当 `enableHistory=true` 时，历史工具会读一个**空的临时 `thesis-review.sqlite`**，报 0 候选，**误导模型**以为学生没有历史——正是第七节警告的失败模式。
- **修复**：
  - 新增 config `workerHome`（默认空），穿透 `WorkerChannel.ensureStarted -> startWorker`；**空 = 全新临时 home**（claim-evidence 模式，无需持久历史，行为不变）。
  - `resolveHistoryHome()` 对齐主项目的 `app_home()`（`python/thesis_review/paths.py`）：`config.workerHome` → `THESIS_REVIEW_HOME` → 平台默认（Windows `%APPDATA%/ThesisReviewAgent`，其余 `~/.thesis-review-agent`）；**只有当该 home 已存在 `thesis-review.sqlite` 且不在 checkout 内**才返回。
  - `enableHistory=true` 但解析不到有效 home → **fail closed**：warn（`history_unavailable`），**不注册**历史工具；核心 claim-evidence 工具照常注册。解析到则把 worker 钉在该 home，历史工具读真实库。
  - 保留 `unsafe_home` 守卫：`workerHome` 指向 checkout 内一律拒绝。
- **验证**：plugin-smoke 新增 2 测（无真实库→历史工具缺席+warn+核心工具仍在；临时库含真实 `thesis-review.sqlite`→历史工具注册）。
- **commit**：`da46ba1`

### Checklist 14（十四）package-lock 清理 — 详情
- **问题**：(1) 全部 **111** 条 `resolved` 指向 `registry.npmmirror.com`，0 条官方源，项目无理由用镜像；(2) lock 根声明 `typescript ^5.6.0`，而 package.json 是 `^5.7.0`——lock 相对 manifest **过期**。（与任务描述的现象一致。）
- **修复**：把 node_modules 移开做**全新** `npm install --registry=https://registry.npmjs.org/`，强制 npm 重新解析并从官方源重下每个包。**未手改 lockfile**。
- **结果**：111/111 `resolved` 在 `registry.npmjs.org`、111 条 integrity、lock 根 `typescript` 同步为 `^5.7.0`（解析 5.9.3）、lockfileVersion 3、包树不变（63 包）。安装后 `@deepseek-ai/*` 版本与之前一致（cordis 4.0.2 / dsh-tools 0.0.1-rc.1 / dsh-system-prompt 0.0.1-rc.5 / schemastery 3.18.2），无漂移。
- **验证**：`npm run typecheck`、`npm run build`、全部 39 测、`npm ci` 均通过。
- **commit**：`4f1ace6`

### Checklist 5 / 十 — 真实 DSH 插件加载（核心要求）
不止 `apply()→fake ctx.tools→registered`。用**真实 `dsh` CLI**验证：
1. `dsh plugin --profile thesis add /data/work/thesis-review-dsh-plugin` → 真实 CLI（转发 pnpm）把插件 **link 进 profile**。
2. `dsh --profile thesis --dump-config` → 组合配置里出现 thesis-review-dsh-plugin 行，thesisReviewAgentPath 指向主仓库。
3. 真实 `dsh --profile thesis` 启动，probe dump `ctx.tools.schemas()`：total tools 33，thesis tools 7，boot 0 error。
- 仓库内固化：`scripts/dsh-smoke.mjs`（`npm run smoke`）。
- **commit**：`bd46955`

---

## 三、真实测试数字

`node scripts/report.mjs`：Unit 13 PASS；Integration 19 PASS；Resilience 7 PASS；Real DSH loader 3 PASS；Real DSH dispatch 3 PASS；Real model SKIPPED。
`npm ci` / `typecheck` / `npm test` 39 测 PASS。真实模型 E2E NOT RUN（无 API key）。

## 四、主仓库

`thesis-review-agent`：**未修改**。HEAD `6a0773a`。所有修复都在 adapter 仓库。

## 五、Git

本轮最终本地 commit（归档当时）：`d566c48261ce92e83fa31fa6e8680d7a219a5702`。本次上传为 GitHub `main` 上的正常同步 commit，未 force push。
