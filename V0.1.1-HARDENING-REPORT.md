# V0.1.1 真实集成加固 — 最终报告

仓库：`thesis-review-dsh-plugin`（分支 `main`）
主仓库 `thesis-review-agent`：**未修改**（HEAD 全程 `6a0773a`，`git status` 全程 clean）
本轮最终 commit：`d566c48`
运行环境：Node v22.19.0（DSH 官方要求 v22.19+/v24）

---

## 一、真实发现并修复的 Bug

### Bug A — systemPrompt 未注入即读取，真实 DSH 启动崩溃
- **修复**：用 `ctx.reflect.get('systemPrompt', false)` 代替直接属性读取；`systemPrompt` 不放入 `inject`。
- **commit**：`a61e144`

### Bug B — 跨 Session worker 状态串档
- **修复**：按 `exec.agent.id` 路由到每 session 独立 WorkerChannel；监听 `agent/disposed` 回收。
- **commit**：`cf3d41c`

### Bug C — Worker 结构化错误码到不了模型
- **修复**：`WorkerError.domain`；domain 拒绝返回 `{ok:false, error:{code,message}}`。
- **commit**：`715d76e`

### Bug D — stdio 启动永久挂起
- **修复**：`startupTimeoutMs` 传进 stdio 分支，超时 `worker_startup`。
- **commit**：`b3bb273`

### Bug E — worker 崩溃后 EPIPE + pending 永挂
- **修复**：write try/catch + stdin/exit/stdout/socket `failAll`。
- **commit**：`b3bb273`

### Bug F — 不理 `exec.signal`
- **修复**：AbortSignal 传透到 LineProtocol.call；触发时丢弃该条 pending。
- **commit**：`b3bb273`

---

## 二、Checklist 结论

| Checklist | 结论 |
|---|---|
| Session 隔离 | FIXED |
| History workerHome | FIXED（fail closed） |
| 结构化 Worker 错误 | FIXED |
| 超时 / abort / crash / dispose | FIXED |
| 真实 DSH 插件加载 | PASS |
| Evidence Gate | 保持 |
| 真实模型 E2E | NOT RUN |
| package-lock | FIXED（npmjs；远端完整 lock 需本机补推） |

History fail-closed：`enableHistory=true` 但解析不到已存在的 `thesis-review.sqlite` 则不注册历史工具。

---

## 三、真实测试数字（归档环境）

| 层 | passed | failed | skipped | 结论 |
|---|---|---|---|---|
| Unit | 13 | 0 | 0 | PASS |
| Integration | 19 | 0 | 0 | PASS |
| Resilience | 7 | 0 | 0 | PASS |
| Real DSH loader | 3 | 0 | 0 | PASS |
| Real DSH dispatch | 3 | 0 | 0 | PASS |
| Real model | 0 | 0 | 1 | SKIPPED |

---

## 四、真实模型 E2E

**NOT RUN**（无 DSH 模型 API key）。

---

## 五、主仓库

`thesis-review-agent`：**未修改**。HEAD `6a0773a`。

---

## 六、Git

归档本地最终 commit：`d566c48261ce92e83fa31fa6e8680d7a219a5702`。
GitHub `main` 为后续正常同步 commit，未 force push。

---

## 七、停止条件

DSH 真实加载、7 thesis 工具、session 不串、Worker 真实运行、Evidence Gate 拒绝编造引文、崩溃/超时不永久挂起、commit 产出 Word + findings.json：均已达成。未做 EXE。

---

## 八、复核后追加修复的 4 个残留问题（非 P0）

主仓库仍未改（HEAD `6a0773a`）。

### 残留 1 — abort 只让 JS 停止等待，未阻止 Python 执行写操作
- **根因**：Harness 超时/abort 只把 JS 侧的等待取消；worker 协议没有 cancellation，Python 会把 op 跑到完。导航读（list_outline/read_section/read_paragraphs/find_text）不会改 findings 或工作文档，但仍可能消耗一次 navigation-budget（Python Worker 在 dispatch 前递增 `nav_calls`）；写操作（open_draft/record_argument_finding/confirm_history_finding/commit_review）则可能在“已告诉模型失败”之后仍写入。
- **修复**：MUTATING abort 调用 `taintAfterAbortedWrite()` 回收 Worker；非写读 abort 可保留当前 Worker。
- **局限**：只重置内存态。已完成的 `commit_review` 落盘无法事务回滚。`confirm_history_finding` 不应被描述成一定会新增 SQLite 记录。

### 残留 2 — stdin EPIPE 未 failAll
- **修复**：`child.stdin.on('error')` 调 `proto.failAll(...)`。

### 残留 3 — Node engines 过时
- **修复**：`engines.node` = `>=22.19`，README 22.19+/24+。

### 残留 4 — dsh-smoke preset 断言过弱
- **修复**：`systemPrompt.assemble({})`，断言 `thesis-review:claim-evidence` section 文本与 preset 一致。
