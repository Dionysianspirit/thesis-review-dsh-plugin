# V0.1.1 真实集成加固 — 最终报告

仓库：`thesis-review-dsh-plugin`（分支 `main`）
主仓库 `thesis-review-agent`：**未修改**（HEAD 全程 `6a0773a`）

---

前半部为 V0.1.1 加固记录（session 隔离、structured domain errors、stdio timeout、crash/EPIPE failAll、AbortSignal、history fail-closed、真实 DSH smoke）。以下为四 Bug 修复之后的 review 收尾。

## 八、复核后追加修复的 4 个残留问题（非 P0）

上一轮报告后复核发现 4 个残留问题，均已在插件仓库内修复并验证；主仓库仍全程未改（HEAD `6a0773a`）。

### 残留 1 — abort 只让 JS 停止等待，未阻止 Python 执行写操作
- **根因**：Harness 超时/abort 只把 JS 侧的等待取消；worker 协议没有 cancellation，Python 会把 op 跑到完。导航读（list_outline/read_section/read_paragraphs/find_text）不会改 findings 或工作文档，但仍可能消耗一次 navigation-budget（Python Worker 在 dispatch 前递增 `nav_calls`）；写操作（open_draft/record_argument_finding/confirm_history_finding/commit_review）则可能在“已告诉模型失败”之后仍写入，导致 worker 内存状态领先于模型的认知。
- **修复**：`WorkerChannel.call` 捕获 `worker_aborted`，若为 MUTATING op 则调用 `taintAfterAbortedWrite()`：同步丢弃 handle、后台 dispose 旧进程，下次调用惰性重启一个干净 worker；非写读 abort 可保留当前 worker。
- **已记录的局限（P1/P2，非 PoC 阻塞）**：只重置**内存态**。中止的 mutating 调用可能让进程内 Worker 或工作文档处于不确定状态，因此适配器回收 Worker。已完成的 `commit_review` 落盘文件无法由适配器事务回滚。`confirm_history_finding` 是读取已确认历史 issue、构造 finding 并应用到工作文档 / 内存 findings，不应被描述成一定会新增 SQLite 记录。彻底修复需要 worker 协议内建 cancellation。

### 残留 2 — stdin EPIPE 守卫只吞异常，未 failAll
- **修复**：`child.stdin.on('error')` 调 `proto.failAll(new WorkerError('worker_write_failed', ...))`，所有 pending 调用立即 reject。

### 残留 3 — README / engines 声明的 Node 版本过时
- **修复**：`engines.node` 改 `>=22.19`，README 改为 22.19+/24+。

### 残留 4 — dsh-smoke preset 断言过弱
- **修复**：调用真实 `systemPrompt.assemble({})`，断言 assembled sections 中存在 `thesis-review:claim-evidence` 且 text 与 preset 一致。
